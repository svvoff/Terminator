# Контекст текущей задачи

<!--
Переписывается оркестратором при каждом выборе задачи — до того, как написан пакет.
Две функции: видно, на что потрачен контекст, и файл переживает переезд на новую сессию
(migration-prompt ссылается на него).

Держи его коротким. Если он разрастается в пересказ карточки — он больше не выполняет ни одну
из двух функций.
-->

## Состояние на 2026-09-18

**TASK-101 (секция «Focus» в поповере) в `in-progress/` с 2026-09-18. Код фазы A принят в тот же
день, ревью дефектов не нашло; карточка ждёт фазы B.** Продвинута по
слову автора прямо из `deferred/`. Автор спросил, зачем ждать 22-го, если UI можно делать на
синтетике, и вопрос показал, что запрет был сформулирован про **карточку**, а относится к двум
**действиям**: пересборке бандла и запуску. Карточка разделена:

- **Фаза A — сейчас:** код и тесты на синтетических свёртках, `swift build`, фильтрованный
  `swift test` без `ConfigStoreTests`. Без `./build.sh` и без запуска. Заканчивается ревью кода, а
  не приёмкой.
- **Фаза B — после того, как автор подтвердит седьмой день:** сначала посуточная таблица Stage 1
  по копии `focus.json`, потом сборка, замена резидента и ручной чеклист из 12 пунктов.

Поправка записана в `roadmap/stages/02-statistics.md`, раздел «Amendment · 2026-09-18».

**Stage 1 открыта на критериях 2 и 3.** Проверено утром 2026-09-18: резидент один (pid 925,
PPID 1, старт 2026-09-16 16:25:35), бинарь бандла от 2026-09-15 13:20, в `config.json` три
правила. В `focus.json` есть 09-15…09-17, сегодня записи ещё нет, но сбор идёт: в журнале есть
утренние строки `frontmost changed`. Новых перезагрузок после двух 16-го нет.

## Закреплено для пакета

- **Решения:** DEC-004, DEC-005, DEC-006, DEC-008. TASK-101 вписана в `applies_to` DEC-004, 005
  и 006 и в роутер 2026-09-18. DEC-008 связывает через EPIC-04 и назван в `context_refs`
  карточки, как у TASK-108.
- **Findings:** §7 (`open`, не `swift run`; без `Bundle.module`), §8 (лейбл меню-бара — почему
  не-цель), §12 (два экземпляра; cdhash и запуск через launchd), §14 (`privacy: .public`). Высота
  `MenuBarExtra(.window)` при росте контента не измерена нигде.
- **Решено в карточке, а не отложено:** слепое пятно 3 TASK-108 (`totalText` падает выше
  `Int64.max` с) закрывается насыщением в обоих форматтерах. Складка делает вызов достижимым
  из файла, который пользователя приглашают править, а падение резидента выключает лимитер до
  следующего входа.

## Ловушки, которые пакет обязан назвать

1. Снимок = `recorded + accrued` при **одном** `Now`. Только файл отстаёт до 60 с, только
   движок теряет прошлые запуски.
2. Второй `focusStore.load()` или второй `FocusStore` — отказ в приёмке.
3. Карантин фокуса не должен выглядеть как тонкая неделя.
4. Сводка не строится в теле `TimelineView`, только при открытии и при раскрытии.
5. Имена резолвятся для объединения ключей правил и строк сводки.

## Прочитано в этой сессии

- Tier 0 целиком; `migration-prompt.md`; `execution-policy.md`, `EXECUTOR.md`,
  `task-packet-template.md`, `handoff/README.md`, `review-checklist.md`, скилл executor.
- TASK-101 (deferred-вариант), TASK-108 (done), `02-statistics.md`, EPIC-04, `decisions/index.md`;
  фрагменты DEC-004, 005, 006 и 008 про связь с Stage 2.
- Исходники: `FocusSummary`, `FocusStore`, `Now`, API `FocusRollup`, `PopoverView`, начало
  `PopoverModel` (строки 36–195), фрагменты `WatchController` (focus, `currentNow`,
  `reloadFromDisk`, `flushFocus`), `TerminatorApp`, `LoggingIdentity`, баннер `PopoverViewModel`;
  `Package.swift`; `build.sh` (шаги копирования).
- Findings §8 и §14; plist автозапуска (путь программы).

## Намеренно не прочитано

`tasks/done/` (кроме TASK-108), остальные `tasks/deferred/`, `epics/` кроме EPIC-04,
`decisions/archive/`, `execution-log/archive/`, `project-brief.md`, `assumptions.md`,
`configurator-input.md`, findings кроме §8 и §14 (§7 и §12 известны по Tier 0),
`PopoverViewModel` и тесты целиком, `FocusSummaryTests`.

## Что осталось открытым для фазы B

- Скачок первого кадра: до `popoverDidOpen()` рисуется снимок прошлого открытия.
- Кликабелен ли заголовок `DisclosureGroup` или только треугольник.
- Высота `MenuBarExtra(.window)` при раскрытии — единственное неизмеренное платформенное
  поведение; если измерится, оно идёт в findings рукой оркестратора.

## Незакоммиченное

Ничего. Промоушен TASK-101, код и тесты фазы A и все документы 2026-09-18 ушли одним коммитом
«TASK-101 фаза A: секция «Focus» в поповере, код принят; карточка ждёт фазы B».
