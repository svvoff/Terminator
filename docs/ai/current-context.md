# Контекст текущей задачи

<!--
Переписывается оркестратором при каждом выборе задачи — до того, как написан пакет.
Две функции: видно, на что потрачен контекст, и файл переживает переезд на новую сессию
(migration-prompt ссылается на него).

Держи его коротким. Если он разрастается в пересказ карточки — он больше не выполняет ни одну
из двух функций.
-->

## Выбранная задача

**TASK-001** — Spike: signing identity, Apple Events consent, hand-rolled quit.
Карточка: `docs/product/backlog/tasks/done/2026-08/TASK-001-spike-signing-consent-quit.md`.

## Почему выбрана

P0, `depends_on: []`, `risk: high`. Все семь остальных готовых карточек зависят от неё прямо
или транзитивно (002→001; 003→002; 004→001,003; 005→001,003; 006→003,004,005; 007→003,004;
008→002), поэтому альтернативного первого шага в ready-пуле не существует.
`scripts/verify-docs.sh` — зелёный (0 ошибок, 0 предупреждений) на момент выбора.

## Прочитано

- Tier 0: `current-state.md`, `backlog/index.md`, `execution-state.md`, `validation/index.md`.
- Tier 1: карточка TASK-001, `decisions/index.md`, DEC-002, DEC-007,
  `docs/ai/execution-policy.md`.
- Findings по номерам: **§4** (что на самом деле делает `terminate()`), **§5** (согласие Apple
  Events), **§6** (подпись), **§7** (упаковка и activation policy), **§9** (часы), **§10**
  (идентичность, pid не кэшируется), плюс секция «Open questions routed to TASK-001».

§7 и §9 не названы в `context_refs` карточки, но взяты сознательно: §7 определяет, запустится
ли пробник как бандл и увидит ли он свой bundle id, §9 — каким семейством часов мерить
длительность `AESendMessage` в трайле B.

## Намеренно не прочитано

`tasks/done/`, `tasks/deferred/` (TASK-101…107), `epics/`, `decisions/archive/`, стадии
роадмапа 2–4, `execution-log/archive/`, `project-brief.md`, `assumptions.md`,
`configurator-input.md`, findings §1–3, §8, §11–14.

## Закреплено для пакета

- **DEC-002** — quit-примитив и политика ретраев.
- **DEC-007** — форма сборки и подпись; TASK-001 — его Review trigger.

Сверено по колонке «Applies to» в `decisions/index.md`: TASK-001 назван ровно в этих двух
карточках, что совпадает с `context_refs` задачи. Расхождений нет.

- Findings **§4**, **§5**, **§6** — три секции, в которые уходит результат; **§10** — правило
  «перечитывай pid в момент использования»; **§7**, **§9** — как выше.

## Маршрут исполнения

**inline.** Проектное исключение из `execution-policy.md`: TASK-001 не делегируется —
интерактивные диалоги согласия требуют человека за клавиатурой, субагент их не увидит и
отчитается об успехе. Пакет в `handoff/` не пишется: исполнителя нет, оркестратор ведёт
трайлы сам вместе с пользователем.

## Гейты риска

Разрешение пользователя получено 2026-08-26 на четыре зоны: **1** (подпись и идентичность),
**2** (Keychain — создание `Terminator Dev`), **3** (TCC — `tccutil` только с
`com.svvoff.terminator.probe`), **4** (завершение чужого процесса — только TextEdit).

Стоп-условие карточки: отрицательный ответ на вопрос 1 → TASK-002 не начинается, карточка
уходит в `blocked/`, DEC-007 идёт на пересмотр. Обходной путь (другая схема подписи) запрещён.

## Требования валидации

`validation_profile: [manual-checklist]` — L5, единственный доступный уровень: `swift build`,
`swift test` и `./build.sh` не существуют до TASK-002.

Жёсткое правило карточки: **каждый** трайл предваряется `tccutil reset` и проверкой, что
`AEDeterminePermissionToAutomateTarget(askUserIfNeeded: false)` вернул `-1744`. Любое другое
значение воид-ит трайл — он не запускается и его результат не докладывается.

## Состояние

**TASK-001 принята (ACCEPT) 2026-08-27**; измерения проведены 2026-08-26 — сессия шла через
полночь. Карточка в `tasks/done/2026-08/`.

Все четыре вопроса измерены; findings §4, §5, §6 переписаны, UNSETTLED-маркеров не осталось,
секция открытых вопросов заменена на «Questions TASK-001 settled». Ревью прошло отдельным
ходом и нашло шесть дефектов в правках findings — все исправлены до приёмки.

Сертификат `Terminator Dev` создан и валиден до 2036-08-23
(SHA-1 `74:D5:82:91:1C:D0:B2:C7:FF:39:61:AF:4B:B0:56:1E:FD:6A:8F:24`, login keychain).

Машина замера: macOS **26.5.2** (25F84), arm64, Swift 6.2.4.

Шесть эскалаций разобраны с пользователем 2026-08-27; таблица решений — в
`execution-state.md`, подробности — в `docs/ai/handoff/current-execution-report.md`.
Две выполнены (дев-луп в §7 и `current-state.md`; `check-forbidden.sh`), одна породила
**TASK-009**, три отложены. Ни одна карточка решения не правилась.

Следующая задача — **TASK-002**, не заблокирована. **TASK-005 не берётся до TASK-009.**
