# Контекст текущей задачи

<!--
Переписывается оркестратором при каждом выборе задачи — до того, как написан пакет.
Две функции: видно, на что потрачен контекст, и файл переживает переезд на новую сессию
(migration-prompt ссылается на него).

Держи его коротким. Если он разрастается в пересказ карточки — он больше не выполняет ни одну
из двух функций.
-->

## Состояние на 2026-08-29

**Весь Stage 1 в `in-progress/`, `ready/` пуст.** Три карточки, и разбирать их надо в разном
порядке.

| Карточка | Код | Чего ждёт |
|---|---|---|
| **TASK-006** — поповер | принят 2026-08-29 | 12 пунктов чеклиста, автор |
| **TASK-008** — автозапуск | принят обеими половинами | 6 пунктов чеклиста, автор |
| **TASK-007** — учёт фокуса | **не исполнялась** | решения автора, запускать ли |

Файлы хендоффа разведены суффиксом: `-popover`, `-loginitem`, `-focus`. Без суффикса —
закрытая TASK-004.

## Следующее действие — две проверки, не восемнадцать пунктов

Так решено с автором 2026-08-29: сначала короткие проверки, потом остальное. Причина в том,
что **две вещи в уже написанном коде не проверены по конструкции, и обе живут в
`TerminatorApp.swift`** — файле, который правила бы TASK-007:

1. срабатывает ли `.onAppear` содержимого `MenuBarExtra` на **каждое** открытие поповера — на
   нём висит `reloadFromDisk()` (пункт 14 TASK-006) и всё чтение статуса автозапуска (TASK-008);
2. перерисовывается ли лейбл меню-бара от `@Observable`-модели — findings §8 измерил
   реактивность на `@State`, форма другая, подтверждения нет.

Упадёт любая — починка ляжет ровно туда, где работала бы TASK-007. Поэтому запуск TASK-007
держится до результата проверок.

## Что решено при сборке пакетов и записано амендментами

- **TASK-006, амендмент 2:** карточка была неисполнима; шесть поимённых швов в файлах TASK-004,
  предикат красных глаз, обязательность кнопки «Quit Terminator», поверхность отказа.
- **TASK-008, амендмент 1 + раунд 2:** владение строкой автозапуска; тумблер отражает нашу
  регистрацию, текст — мнение системы; из `.disabledByUser` пути в `enable()` нет по конструкции.
- **TASK-007, амендмент 1:** инструкция про `.log` неисполнима; шесть швов названы; чтение файла
  фокуса — не «восполнение истории»; удержание всех дней.
- Все три черновика пакетов прошли враждебное ревью **до** делегирования, и каждое нашло
  дефекты, которые стоили бы раунда. Третье забраковало пакет целиком, и справедливо.

## Систематический дефект документов

Три карточки из трёх (TASK-004, TASK-006, TASK-007) не называют DEC-008 в `context_refs`, хотя
роутер числит его за каждой. Дважды обратное: карточка называет решение, которого роутер за ней
не числит (TASK-006 → DEC-006, TASK-007 → DEC-001), и оба раза по существу права. Правка
фронтматтера карточек решений — не работа оркестратора в одиночку.

## Прочитано

- Tier 0 целиком, роутер решений, `handoff/README.md`, `task-packet-template.md`,
  `validation/index.md`.
- Карточки TASK-006, TASK-007, TASK-008 целиком со всеми амендментами.
- Findings по номерам: §1, §2, §7, §8, §9, §10, §11, §12, §13, §14.
- Исходники: весь `Sources/`, включая новый код TASK-006 и TASK-008.

## Намеренно не прочитано

`tasks/done/`, `tasks/deferred/`, `epics/`, `decisions/archive/`, стадии роадмапа,
`execution-log/archive/`, `project-brief.md`, `assumptions.md`, `configurator-input.md`,
findings §3, §4, §5, §6.

## Гейты риска

- **TASK-006** и **TASK-007** — `risk: medium`, отдельного разрешения не требуют. У TASK-007 есть
  оговорка про зону 6: она заводит новый человекочитаемый контракт на диске (файл фокуса), формат
  правил не меняет, и контракт утверждается при приёмке по дословным байтам — как в TASK-003.
- **TASK-008** — зона 5, разрешение получено 2026-08-28 и **ограничено кодом и тестами**. В
  настоящий `~/Library/LaunchAgents` не записано ничего; первая настоящая запись plist произойдёт
  на ручном чеклисте, руками автора.
