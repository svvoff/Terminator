# Журнал исполнения — текущая веха

Одна запись на принятую задачу. Здесь же лежат **сырые транскрипты измерений**: TASK-001 прямо
требует, чтобы выводы команд и тексты диалогов согласия попадали сюда, а в
`docs/product/recon/macos-findings.md` уходили только выводы. Findings читает каждая
последующая задача, поэтому он обязан оставаться коротким; журнал — нет.

Из этого следует правило ротации: журнал **не** обязан быть компактным, но обязан быть
навигируемым. Когда `latest.md` перестаёт читаться (ориентир — 400 строк) или закрывается
веха, записи переезжают в `archive/YYYY-MM.md`, а здесь остаются только записи текущей вехи.
Архив не читается по умолчанию.

Резюме принятой задачи в одну строку живёт в `../execution-state.md` — журнал не является
маршрутизирующей поверхностью и не читается для выбора следующей задачи.

Формат записи:

```markdown
## YYYY-MM-DD — TASK-XXX — <заголовок> — ACCEPT

- Что сделано: 1–3 строки.
- Валидация: команды и результаты; для manual-checklist — кто выполнял и что увидел.
- Решения и отклонения: что пришлось решить по ходу; что эскалировалось.
- Транскрипты: ниже, под свёрткой, дословно.
```

---

## Ротация

В `archive/2026-08.md` вытеснены пять записей, все по правилу 400 строк:

- **TASK-001** — спайк подписи, согласия Apple Events и вежливого quit (ACCEPT 2026-08-27).
  Выводы живут в findings §4, §5, §6; там же **сырые транскрипты, на которые ссылается
  раздел «Questions TASK-001 settled»** в findings.
- **TASK-002** — SwiftPM-пакет, сборка бандла, подпись, череп в меню-баре (ACCEPT 2026-08-27).
  Выводы живут в findings §6 и §8, а форма сборки — в `current-state.md`.
- **TASK-003** — модель правила и долговечное хранилище конфига (ACCEPT 2026-08-27).
  Там же **канонические байты формата v1** и контракт `writeDurably(_:to:)`, на которые
  ссылаются `current-state.md` и `execution-state.md`.
- **TASK-009** — спайк согласия на quit на пяти приложениях (ACCEPT 2026-08-28).
  Выводы живут в findings §4 и §5; в архиве — семнадцать транскриптов отправок.

- **TASK-004** — движок наблюдения: KVO + сверка, дедлайны, сменная стратегия (ACCEPT
  2026-08-29). Там же **восемь транскриптов ручного чеклиста**, включая оба прогона пункта 8
  (троттлинг), и разбор дефекта фантомной сессии, стоившего второго раунда.

В `archive/2026-09.md` вытеснены три записи:

- **TASK-007** — учёт фокуса и дневная свёртка (ACCEPT 2026-09-01, код принят 2026-08-29).
  Там же **транскрипты всех семи пунктов ручного чеклиста**: множество причин паузы на сне и
  замке, контроль 2×2 «замок против смены сессии», изоляция хука `applicationWillTerminate`,
  лестница попыток quit до `attempts-exhausted`. Выводы, переживающие задачу, — в findings §15.
- **TASK-006** — меню-барный поповер (ACCEPT 2026-09-14, код принят 2026-08-31 по шести
  раундам). Там же **раунды 2–6 целиком** — четыре дефекта, найденных ручным чеклистом в слое
  встречи с AppKit, — и транскрипты закрывающего прогона: пустое состояние, редактор лимита,
  порядок строк реверсом порядка добавления, карантин, два цикла перезапуска Finder системой.
- **TASK-008** — автозапуск через LaunchAgent (ACCEPT 2026-09-14, код принят 2026-08-29).
  Там же измерение вилки устаканивания статуса (0, 29 мин], разбор дефекта ложного обещания
  «вступит при следующем входе» и разбор `Launch Constraint Violation`: механизм установлен —
  чистая пересборка двигает cdhash и launchd убивает запуски, — а неизвестным осталось лишь то,
  почему в итоге проходит третья попытка.

За дословными транскриптами — в архив. Ротация 2026-08-28 увезла TASK-003 и TASK-009 разом;
ротация 2026-08-31 увезла TASK-004, когда `latest.md` дорос до 980 строк при ориентире 400;
ротация 2026-09-01 увезла TASK-007 сразу после приёмки, с 864 строк до 517; **ротация 2026-09-14
увезла TASK-006 и TASK-008 по второму основанию контракта — закрытию вехи**, с 834 строк до
шапки. Все увезённые записи на девять десятых состоят из транскриптов, которые читаются один раз.

**Здесь не осталось незакрытой работы: все карточки Stage 1 приняты 2026-09-14.** `ready/` и
`in-progress/` пусты. **Веха при этом открыта** — **два** из пяти критериев выхода стадии не
выполнены (`roadmap/stages/01-mvp.md`): 2 и 3, обычная неделя с тремя правилами и её посуточные
данные. Критерий 1 закрыт 2026-09-15 записанным supersede (запись ниже). Вся история вехи лежит в
`archive/2026-08.md` и `archive/2026-09.md`; здесь останутся только шапка, эта сводка и записи
вехи, не относящиеся к карточкам, пока не появится новая незакрытая работа.

---

## 2026-09-15 — Stage 1, критерий 1 — supersede для TASK-005 — ЗАПИСАНО

Не приёмка карточки: карточек в работе нет. Запись вехи.

- **Что сделано.** Написан supersede, которого требовал критерий 1: `roadmap/stages/01-mvp.md`,
  секция Exit criteria, подраздел «Supersede · 2026-09-15 — criterion 1, TASK-005». Критерий
  переформулирован как TASK-001…004 и TASK-006…008. TASK-005 остаётся в `tasks/deferred/` и в
  `done/` **не** переезжает: диффа и доказательств нет, запись в `done/` заявляла бы работу,
  которой правильно не было. На карточку TASK-005 добавлена ссылка на этот supersede.
- **Основание.** TASK-009 (ACCEPT 2026-08-28): 17 отправок на пяти приложениях в трёх состояниях
  согласия, включая явный запрет, — подопытный умирал каждый раз, путь quit согласия не
  спрашивает (findings §5). Уцелевшая часть карточки, `NSAppleEventsUsageDescription`, уехала в
  `Packaging/Info.plist` ещё с TASK-002. Решение отложить принял автор 2026-08-28; supersede
  ничего в продукте не меняет, он записывает это решение на том уровне, где сверяется критерий.
- **Почему не DEC.** Правится один критерий одной стадии; DEC связывает продуктовую механику
  поперёк задач. Если исключение должно связывать и за пределами Stage 1 — это отдельное решение
  и его надо обосновывать как решение.
- **Дубль экземпляров: пересмотрен автором, принят как есть.** Основание от 2026-09-14 («это
  только дев-луп») опровергнуто и заменено честным: принятый дефект на пользовательском пути,
  одноэкземплярность не реализована и не обещана. Других оснований автор не приводил, и они не
  дописаны за него. Размер ущерба остаётся неизмеренным — контрольного опыта «два экземпляра
  против одного» не ставилось. Переоткрытие — при заметной просадке дневной записи фокуса или
  дубле вне дев-лупа; триггер предложен оркестратором.
- **Состояние вехи после записи.** Критерии 1, 4, 5 выполнены. 2 и 3 — нет, и **отсчёт недели не
  начат**: на 2026-09-15 в `config.json` два правила (Telegram 360 с, TextEdit 5400 с), причём
  TextEdit попадает в запись фокуса лишь в трёх днях из одиннадцати, по 0–4 минуты. `focus.json` —
  одиннадцать дней, три приложения ровно в одном из них.
- **Валидация.** `scripts/verify-docs.sh` — 0 ошибок, 0 предупреждений. Кода правка не касается;
  сборка и тесты не перезапускались намеренно — ни один файл в `Sources/`, `Tests/`,
  `Package.swift` или `build.sh` не тронут.
