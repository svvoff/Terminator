# Состояние исполнения

Компактное состояние цикла доставки. Читается первым делом (Tier 0) и обновляется после каждой
принятой задачи. Здесь нет статусов карточек — статус это каталог, в котором лежит карточка.

## Активный исполнитель

`claude-code` — субагент через `.claude/skills/executor/`, контракт `docs/ai/EXECUTOR.md`.

Смена исполнителя возможна только на границе задач и записывается здесь.

## Активный ревьюер

Нет. Ревью выполняет оркестратор отдельным ходом; порог обязательности — в
`docs/ai/execution-policy.md`.

## Режим оркестратора

primary. Резервный оркестратор в проекте не настроен.

## Текущая веха

Stage 1 — MVP: ограничитель плюс молчаливый сбор focus-данных.
Стадия: `docs/product/roadmap/stages/01-mvp.md`.

## Текущая задача

Нет. Следующая по плану — **TASK-001** (спайк: подпись, согласие Apple Events, вежливый quit).
Она первая намеренно: все остальные готовые карточки зависят от неё прямо или транзитивно.

TASK-001 не делегируется исполнителю — она требует человека за клавиатурой (диалоги согласия).

## Результат последнего исполнения

Нет. Кода в репозитории ещё нет.

## Недавно завершённые задачи

Нет.

## Заблокированные задачи

Нет.

## Известные базовые проблемы

Нет — сборки ещё не существует.

## Состояние валидации

`swift build`, `swift test` и `./build.sh` недоступны: нет `Package.swift` и нет `build.sh`,
их создаёт TASK-002. До этого единственный работающий уровень — ручной чеклист.

`scripts/verify-docs.sh` — зелёный (0 ошибок, 0 предупреждений).

## Открытые вопросы, гейтящие работу

Четыре вопроса из `docs/product/recon/macos-findings.md` («Open questions routed to TASK-001»),
два из них помечены там UNSETTLED. Пока они не закрыты, движок против них не пишется.

Если ответ на вопрос 1 (переживает ли TCC-грант пересборку под самоподписанным сертификатом)
отрицательный — TASK-002 не начинается, карточка уходит в `blocked/`, DEC-007 идёт на
пересмотр.

## Напоминание о бюджете контекста

Сначала индексы. Findings — по номерам секций, которые называет карточка. Архивы не читаются.
