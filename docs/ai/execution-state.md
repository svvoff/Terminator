# Состояние исполнения

Компактное состояние цикла доставки. Читается первым делом (Tier 0) и обновляется после каждой
принятой задачи. Здесь нет статусов карточек — статус это каталог, в котором лежит карточка.

## Активный исполнитель

`claude-code` — субагент через `.claude/skills/executor/`, контракт `docs/ai/EXECUTOR.md`.

Смена исполнителя возможна только на границе задач и записывается здесь.

## Активный ревьюер

Нет. Ревью выполняет оркестратор отдельным ходом; порог обязательности — в
`docs/ai/execution-policy.md`.

## Режим оркестратора

primary. Резервный оркестратор в проекте не настроен.

## Текущая веха

Stage 1 — MVP: ограничитель плюс молчаливый сбор focus-данных.
Стадия: `docs/product/roadmap/stages/01-mvp.md`.

## Текущая задача

Нет. Следующая по плану — **TASK-003** (модель правила и долговечное хранилище конфига): P0,
`risk: low`, `depends_on: [TASK-002]` — зависимость закрыта. Профиль `swift-build` + `swift-test`,
проверяется целиком без человека за клавиатурой. Это первая карточка, приносящая юнит-тесты.

В `ready/` из восьми карточек зависимости закрыты у трёх: TASK-003 (P0, low), **TASK-008** (P2,
low — автозапуск, зона 5) и **TASK-009** (P0, **high**, зона 4 — закрывает чужие приложения,
нужно явное разрешение пользователя). По политике выбора берётся TASK-003.

**TASK-005 не берётся, пока не выполнена TASK-009.** Решение пользователя от 2026-08-27:
находку «quit не требует согласия» проверить на нескольких приложениях, прежде чем делать из
неё вывод. TASK-009 не блокирует TASK-003, TASK-004 и TASK-007.

## Результат последнего исполнения

**TASK-002 — ACCEPT** (принята 2026-08-27). Три раунда исполнения, каждый отревьюен отдельным
ходом; ревью перепроверяло измерения исполнителя своими прогонами, а не принимало на слово.

- **Раунд 1** — пакет, `build.sh`, подпись, `Info.plist`, глиф, плейсхолдер. Изменений не
  запрошено. Исполнитель обнаружил, что cdhash-гейт, написанный по букве пакета, не срабатывает,
  починил и доложил; отклонение принято — ошибка была в пакете, не в коде.
- **Раунд 2** (амендмент 1) — гейт переписан с блоклиста на fail-closed позитивную проверку
  designated requirement, плюс `--self-test-guard` на синтетических строках.
- **Раунд 3** (амендмент 2) — состояние глаз глифа поднято в `App` и переключается кнопкой:
  Review trigger DEC-009 спрашивает про различимость **на фоне меню-бара**, а раунд 1 отвечал
  на этот вопрос образцами в поповере.

Оркестратор внёс одну правку inline: комментарий в `PlaceholderView.swift`, утверждавший то,
что амендмент 2 опроверг. Только комментарий, с пересборкой после.

## Недавно завершённые задачи

- **TASK-002** — SwiftPM-пакет, сборка бандла, подпись, череп в меню-баре. Первый продуктовый
  код в репозитории. Три измерения ушли в findings (§6 — дважды, §8 — один раз).
- **TASK-001** — спайк подписи, согласия Apple Events и вежливого quit. Все четыре вопроса
  закрыты; три утверждения findings опровергнуты измерением.

## Заблокированные задачи

Нет. Ни одна карточка не ушла в `blocked/`: стоп-условие TASK-001 (отрицательный ответ на
вопрос 1) не сработало — грант переживает пересборку.

**TASK-005 под вопросом, но не заблокирована.** См. эскалацию 1.

## Известные базовые проблемы

- **Три продуктовых документа устарели** из-за измерения TASK-001: **EPIC-02**, **DEC-005**,
  **DEC-006** повторяют фразу «only permission cost is Apple Events». Правка отложена
  сознательно: формулировка зависит от исхода TASK-009. Не переписывать раньше.
- **DEC-002 содержит две неточности**, правка отложена по решению пользователя (2026-08-27):
  `kAENormalTimeout` — несуществующая константа (реальная `kAEDefaultTimeout`), и трактовка
  `-1743` как немедленно терминального при том, что без гранта ожидается `-1744`. findings §4
  и §5 поправлены; сама карточка решения — нет.

## Состояние валидации

Все команды проекта существуют и зелёные на 2026-08-27:

| Команда | Состояние |
|---|---|
| `swift build` / `swift build -c release` | зелёные на чистом дереве, **ноль** строк `warning:` |
| `swift test` | тестов пока нет — первые приносит TASK-003, и это не пробел |
| `./build.sh` | EXIT=0, терминальный шаг `codesign --verify --strict` |
| `./build.sh --self-test-guard` | 4/4 вердикта гейта, EXIT=0, ничего не собирает |
| `./build.sh --adhoc-control` | EXIT=1, `DESIGNATED_REQUIREMENT_MISMATCH` — контрольный опыт |
| `scripts/verify-docs.sh` | 0 ошибок, 0 предупреждений |
| `scripts/check-forbidden.sh` | зелёный |

Designated requirement бандла:
`identifier "com.svvoff.terminator" and certificate leaf = H"74d582911cd0b2c7ff3961af4bb0561efd6a8f24"`
— переживает пересборку побайтово при меняющемся CDHash.

Сертификат **`Terminator Dev`** валиден до 2036-08-23 (SHA-1
`74D582911CD0B2C7FF3961AF4BB0561EFD6A8F24`, login keychain). Его SHA-1 теперь **зашит
константой в `build.sh`**: пересоздание сертификата уронит сборку с явным сообщением, и чинится
это правкой той константы, а не смягчением гейта.

Две ловушки окружения, обе измерены и записаны в findings §6:

- ad-hoc бандл **проходит** `codesign --verify --strict` с нулём — одной верификации мало,
  поэтому гейт стоит до неё;
- `codesign --verify --strict` зависит от доступа к keychain: в песочнице тот же исправный
  бандл падает с `CSSMERR_TP_NOT_TRUSTED`. `codesign -d -r-` там работает, то есть гейт
  переживает, а верификация — нет.

Одноразовый пробник в `probes/task-001/` не трогался и ни одной карточкой не используется.

## Открытые вопросы, гейтящие работу

Четыре вопроса TASK-001 **закрыты**, UNSETTLED-маркеров в findings не осталось. Шесть эскалаций,
которые спайк открыл взамен, разобраны с пользователем 2026-08-27. Их дословное изложение уехало
вместе с отчётом в `docs/ai/execution-log/archive/2026-08.md` — `handoff/current-execution-report.md`
перезаписывается каждым циклом и на них больше не указывает.

| # | Эскалация | Решение пользователя (2026-08-27) |
|---|---|---|
| 1 | Quit не требует согласия Apple Events — измерено на TextEdit | **TASK-009** создана: проверить на 4+ приложениях. TASK-005 ждёт её |
| 2 | Обоснование DEC-007 опровергнуто (ad-hoc тоже сохраняет грант) | отложено; решение устояло на других опорах, TASK-002 не блокировалась и прошла |
| 3 | `kAENormalTimeout` не существует в SDK, DEC-002 его называет | отложено; findings §4 поправлен, DEC-002 — нет |
| 4 | `-1744` против `-1743` в политике ретраев DEC-002 | отложено; findings §5 поправлен, DEC-002 — нет |
| 5 | Дев-луп прямым exec ломает атрибуцию TCC | **сделано**: findings §7 и `current-state.md` поправлены |
| 6 | `check-forbidden.sh` срабатывает на комментарии | **сделано**: комментарии снимаются перед грепом |

Отложенные 2–4 остаются открытыми и живут здесь, чтобы не потеряться. Ни одна карточка решения
не правилась.

**Открыто после TASK-002** — ничего, что гейтит работу. Записано, чтобы не всплыло сюрпризом:

- `./build.sh --adhoc-control` оставляет ad-hoc-бандл в `build/`: гейт падает **после** подписи,
  и уборку скрипт сознательно не делает. После контрольного опыта нужен обычный `./build.sh`.
- SHA-1 сертификата зашит константой в `build.sh`. Пересоздание `Terminator Dev` уронит сборку
  с явным сообщением — чинится правкой константы, не смягчением гейта.
- `Contents/Resources/` собирается пустым: так требует карточка, `codesign` принимает
  (`Sealed Resources … files=0`). Ресурсов у продукта не планируется — иконка рисуется кодом.
- Review trigger **DEC-009** сработал и **закрыт**: автор принял глиф на живой машине в обеих
  темах. Второй триггер этой карточки (Stage 4, товарные знаки) остаётся будущим.

## Напоминание о бюджете контекста

Сначала индексы. Findings — по номерам секций, которые называет карточка. Архивы не читаются.
